# Supabase JDBC (Session pooler) — preferred for Spring Boot / Hibernate
SUPABASE_DB_URL=jdbc:postgresql://aws-0-ap-southeast-2.pooler.supabase.com:5432/postgres?sslmode=require
SUPABASE_DB_USER=postgres.zgwtehnofcahlrzeselb
SUPABASE_DB_PASSWORD=your-database-password

# Optional
PORT=8080
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Frontend (only needed if not using Vite proxy)
VITE_API_BASE_URL=http://localhost:8080
